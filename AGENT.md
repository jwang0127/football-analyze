# 足球预测 Agent 工作指引

本文件是本项目后续每次足球预测、杯赛预测、盘口更新、串关生成和赛后复盘的统一执行规范。

## 一、总原则

预测必须先确认数据，再生成模型结果，最后做页面和风险校验。赔率是重要信息，但不能作为唯一依据。

每次预测都必须区分：

- 90 分钟胜平负结果；
- 加时/点球后的晋级结果；
- 竞彩结算口径与真实比赛走势。

不得把晋级概率直接当作 90 分钟胜率，也不得因为赔率低就输出“稳单”或保证性结论。

固定免责声明：

> 以上仅为公开信息整理后的娱乐分析，不构成任何购彩建议，请理性参考。

## 二、每次预测的标准流程

### 1. 确认业务日和比赛范围

优先使用 Sporttery 的 `businessDate`，不能只按北京时间开赛日期筛选。

检查：

- 比赛业务日；
- 比赛编号和 `matchId`；
- 联赛/杯赛名称；
- 开赛时间；
- 是否重复比赛；
- 是否已经产生赛果；
- 是否属于排除清单。

### 2. 数据来源优先级

按以下顺序使用数据：

1. 官方赛程、官方赛果、官方竞赛规则；
2. Sporttery 竞彩赔率和比分矩阵；
3. 官方俱乐部、联赛、欧足联、足协的伤停、首发、赛制和积分资料；
4. 公开球队近期赛果、进失球和主客场数据；
5. IPA 中发现的 500 系列接口，仅作为经授权后的二级盘口/比分核验源；
6. 新闻、论坛或第三方推荐，只能作为未核验线索，不能直接当成事实。

IPA 中的接口字段包括胜平负、让球、比分、总进球、比赛 ID 和实时比分相关字段，但当前静态分析不能获得稳定的授权请求参数。不得绕过登录、签名、TLS、付费权限或访问控制。

### 3. 基本面检查

每场比赛至少检查并写入：

- 近 5 场进球和失球；
- 近 5 场积分效率；
- 进球率；
- 零封率；
- 双方进球率；
- 大于 2.5 球率；
- 主队近 5 个主场；
- 客队近 5 个客场；
- 距离上一场的休息天数；
- 公开可核验的伤停、停赛和首发信息；
- 赛程密度、旅行和下一场优先级；
- 杯赛阶段、首回合比分和晋级压力。

数据不足时必须显示“基本面数据不足”，保持中性并降低置信度，不能用赔率强行填补缺失事实。

### 4. 盘口和赔率检查

必须同时读取：

- 胜平负 `had`；
- 让球 `hhad`；
- 总进球 `ttg`；
- 比分 `crs`；
- 半全场 `hafu`；
- 开盘到最新快照的变化。

开盘定义为“同日最早已观测快照”，除非有开售前连续采样，否则不能称为真实初盘。

盘口异动必须回答：

- 胜平负方向是否变化；
- 让球是否独立变化；
- 大小球是否与基本面一致；
- 市场是否过度集中在一个结果；
- 市场与基本面发生冲突时，冲突原因是什么。

盘口变化只作为证据，不得单独触发爆冷或大球结论。

## 三、杯赛模型选择

### 欧洲冠军联赛

使用 UCL 专用模型。重点检查联赛阶段、淘汰赛、两回合和首回合比分。未知首发时降低置信度，不能只按豪门赔率扩大主胜。

### 欧罗巴联赛

资格赛和淘汰赛经常存在两回合赛制。必须区分首回合、次回合和附加赛，额外检查弱侧进球能力、客场策略和总进球门控。

### 欧洲超级杯

单场决赛、赛季初、阵容和状态不确定性较高。使用单场专用模型，限制置信度上限，优先输出比分保护和风险说明。

### 英格兰联赛杯

重点检查轮换、青年球员、门将变化、赛程密度和强队下一场优先级。未知首发时施加轮换惩罚，并提高平局/冷门保护。

### 巴西杯

必须检查比赛阶段、主客场、旅行、两回合/单回合规则和晋级策略。不能直接复用联赛模型。

### 亚洲冠军精英联赛

重点检查东西亚差异、旅行距离、休息天数、赛制阶段、主场环境和阵容轮换。数据不足时保持低置信度。

### 南美解放者杯

重点检查旅行、主场环境、阵容轮换、弱侧进球和淘汰赛策略。比分池必须保留平局、双方进球和弱侧偷袭路径，不能只保留热门零封。

## 四、大球和爆冷专项闸门

每场必须单独输出“大球路径”和“爆冷路径”，不能让胜平负模型自动代替它们。

### 大球触发条件

至少满足以下两类证据中的两类，才可以提高 4 球以上或大球尾部权重：

- 双方近况进球率较高；
- 双方大于 2.5 球率较高；
- 双方双方进球率较高；
- 至少一方零封率低；
- 让球方领先后仍有追分或失球风险；
- 盘口大小球与基本面共同支持高节奏。

伤停集中在后场可能提高双方进球和大球，但若伤停集中在双方锋线，则应收缩总进球预期，不能一概判大。

### 爆冷触发条件

只有在弱侧具备至少一项真实进攻能力时，才保留较高爆冷尾部：

- 弱侧近期进球率不低；
- 弱侧客场/主场进攻数据不差；
- 强侧零封率低；
- 强侧存在重要伤停或明显轮换；
- 盘口与基本面出现持续方向冲突；
- 杯赛赛制导致强侧不必全力争胜。

没有弱侧进球能力证据时，只保留低权重冷门路径，不得为了“丰富内容”虚构爆冷。

## 五、标准输出内容

每场页面和预测 JSON 应尽量包含：

- 胜平负概率与方向；
- 让球方向；
- 总进球主选、备选和概率；
- 主比分、备选比分、尾部比分；
- 半全场；
- 基本面摘要；
- 伤停/首发证据状态；
- 大球触发器；
- 爆冷触发器；
- 盘口异动和市场冲突；
- 双方历史交锋：样本数、当前主队视角胜平负、进失球、连续不败、最近5次交手；
- 杯赛赛制输入；
- 置信度和风险等级；
- 缺失分析维度；
- 90 分钟与晋级口径说明。

历史交锋不得凭印象填写。优先按双方球队代码从全部历史赛果中匹配，去重后再计算；双方主客场倒置时必须转换为当前主队视角。历史交锋只能作为背景证据，不能单独覆盖当前实力、阵容、赛制和盘口。样本为零时显示“暂无可核验的双方历史交手”；只有在样本和结果都可追溯时，才允许输出“近N次未负”之类结论。

看板输出硬约束：任何缺失数据都不得渲染为空白。每个缺失字段必须显示缺失原因、当前处理方式和是否影响模型权重。例如缺少上一场就显示“暂无已核验的上一场比赛，暂不做状态传导”；缺少伤停就显示“暂无官方伤停或首发，阵容影响不做硬修正”。逐场卡片必须包含数据覆盖与缺口、历史交锋、基本面层和风险处理说明。

“稳单”只能在证据完整、方向一致、样本外表现支持时使用。否则使用“相对倾向”“风险较高”“需临场复核”等表述。

串关必须遵守项目现有约束：

- 不把低置信度冷门堆叠成所谓稳串；
- 比分串必须有证据门槛；
- 每组串关显示整体风险；
- 不重复同一比赛的互相冲突结论；
- 生成前检查最低赔率、串关腿数和重复比赛。

## 六、模型与代码要求

主要脚本：

- `scripts/build_public_context.py`：生成基本面和赛前证据上下文；
- `scripts/generate_date_pages.py`：生成比赛预测、比分池和页面；
- `scripts/market_movement.py`：计算开盘/最新快照和盘口异动；
- `scripts/backtest_calibration.py`：滚动回测和校准；
- `scripts/daily_automation.py`：日常抓取与更新；
- `scripts/world_cup_review.py` 或对应复盘脚本：赛后复核。

每次模型改动必须：

1. 保持不同赛事模型隔离；
2. 使用滚动样本外回测，不追逐单场结果；
3. 记录模型版本、样本数、参数变化和适用赛制；
4. 对置信度做上限控制；
5. 对小样本使用收缩，不得过拟合；
6. 同时复核胜平负、总进球、比分池和爆冷分层表现。

模型隔离硬约束：每个不同的 `league`/赛事名称都必须拥有自己的模型档案和版本号。禁止因为赛事名称未登记就回退到其他联赛模型；遇到新赛事时，先创建独立的中性起始模型，使用低置信度和零复盘样本标记，后续只用该赛事自己的滚动赛果校准。杯赛、资格赛、联赛阶段和国内联赛即使球队层面存在交集，也必须按赛事分别建模。

新赛事建模前必须先运行 `python scripts/research_competition.py --competition "赛事名称" --output data/competition_research_<slug>.json` 或调用同等研究流程。研究包至少要汇总本地全部历史赛果、主/平/客分布、平均总进球、已出现球队、日期范围、官方赛程/赛制入口和缺失维度。已有样本要用本赛事收缩先验；没有样本才可用中性先验。研究包未完成时，模型必须标记 `researchCompleteness`、`missingDimensions` 和 `matchday_refresh_required`，不得输出高置信度结论。

对于日职、日乙、美职等本地竞彩板块不完整的赛事，必须额外抓取公开赛季赛果。日职和美职可使用 ESPN public scoreboard 作为赛果交叉源；日乙优先使用 J.League 官方赛程、积分和比赛页，不能把日职数据填充到日乙。外部赛果只能补充赛事级先验和球队近期样本，最终预测仍需与当天 Sporttery 比赛编号和赔率逐场对齐。

## 七、预测前后检查命令

预测前建议运行：

```powershell
python scripts/daily_automation.py
python scripts/build_public_context.py --date YYYYMMDD --source data/sporttery_YYYYMMDD_latest.json
python scripts/generate_date_pages.py --date YYYYMMDD --source data/sporttery_YYYYMMDD_latest.json
```

代码或模型修改后必须运行：

```powershell
python -m unittest discover -s scripts -p 'test_*.py' -v
python -m py_compile scripts/*.py
python scripts/backtest_calibration.py --json data/backtest_report.json
python scripts/validate_date_pages.py --date YYYYMMDD --expected-matches N
```

如果官方数据接口延迟，保留最后一次已核验赛果，不能编造新赛果。生成完成后检查：

- 比赛数量；
- 业务日；
- `target_issue`、`next_draw_at` 等仅适用于体彩项目的字段不要误混入足球项目；
- 赔率更新时间；
- 赛果是否误写入预测；
- 90 分钟与晋级字段是否分离；
- 串关是否重复比赛；
- 页面与 JSON 是否一致。

## 八、发布与复盘

发布前先查看 `git status` 和 `git diff`，保留用户已有修改。确认测试、页面校验和生成结果后再发布。

如需推送 GitHub，按项目要求使用 SSH 远程地址，不使用 HTTPS 远程地址。除非用户明确要求，不自动提交或推送。

赛后复盘必须记录：

- 90 分钟胜平负是否命中；
- 总进球是否命中；
- 主比分是否命中；
- 比分池是否覆盖；
- 爆冷路径是否有真实信号；
- 大球/小球判断是否被基本面支持；
- 盘口异动是否具有信息价值；
- 哪些字段缺失导致误判；
- 是否需要调整参数，还是仅仅是正常随机误差。

复盘结论必须基于滚动样本，不能因为一场爆冷或一场大球就修改模型。
